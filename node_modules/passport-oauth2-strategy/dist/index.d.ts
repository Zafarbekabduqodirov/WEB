import * as passport from 'passport-strategy';
export interface IStrategyOptions {
    passReqToCallBack?: boolean;
}
export interface IRequest {
    body: Object;
}
export declare class Strategy extends passport.Strategy {
    name: string;
    private _verify;
    private _passReqToCallBack;
    constructor(options: Function | IStrategyOptions, verify?: Function);
    authenticate(req: IRequest): void;
}
